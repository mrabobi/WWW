@page "/Devices"
@if (Things == null)
{
    <MudGrid>
        <MudItem xs="12">
            <MudProgressLinear Color="Color.Primary" Size="@Size.Large" Indeterminate="true" Class="my-7" />
        </MudItem>
    </MudGrid>
}
else
{
    @*<MudGrid>
        @foreach (var thing in Things)
        {
            <MudItem xs="4">
                <MudCard>
                    <MudCardHeader>
                        <CardHeaderContent>
                            <MudText Typo="Typo.h6">@thing.title</MudText>
                        </CardHeaderContent>
                    </MudCardHeader>
                    <MudCardContent>
                            <MudTable Items="@thing.properties" Dense="true" Hover="true" Bordered="true" Striped="true" Loading="@DataIsLoading">
                                <ToolBarContent>
                                    <MudText Typo="Typo.h6">Properties</MudText>
                                </ToolBarContent>
                                <HeaderContent>
                                    <MudTh Style="text-align:center">Property</MudTh>
                                    <MudTh Style="text-align:center">Description</MudTh>
                                    <MudTh Style="text-align:center">Value</MudTh>
                                    <MudTh></MudTh>
                                    <MudTh></MudTh>
                               </HeaderContent>
                                <RowTemplate>
                                    <MudTd Style="text-align:center">@context.title</MudTd>
                                    <MudTd Style="text-align:center">@context.description</MudTd>
                                    <MudTd Style="text-align:center">@context.LastName</MudTd>
                                    <MudTd Style="text-align:center"><MudButton IconClass="@Icons.Filled.Delete" Color="@Color.Warning" Variant="@Variant.Filled" FullWidth="true" OnClick="() => RemoveUserFromGroup(context.UserId)">Set</MudButton></MudTd>
                                </RowTemplate>
                                <PagerContent>
                                    <MudTablePager />
                                </PagerContent>
                            </MudTable>                     
                    </MudCardContent>
                    <MudCardActions>
                        <MudButton IconClass="@Icons.Filled.Add" Color="@Color.Success" Variant="@Variant.Filled" FullWidth="true" OnClick="()=>ActivateThing(thing)">Activate</MudButton>
                    </MudCardActions>
                </MudCard>
            </MudItem>
        }
    </MudGrid>*@
}